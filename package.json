{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build:frontend": "npm --prefix neko install && npm --prefix neko run build && rm -rf public/neko && mkdir -p public/neko && cp -r neko/build/* public/neko/",
        "build:all": "npm run build && npm run build:frontend",
        "dev:frontend": "npm --prefix neko start"
    },
    "devDependencies": {
        "@popperjs/core": "^2.11.6",
        "@vitejs/plugin-react": "^5.2.0",
        "axios": "^1.1.2",
        "bootstrap": "^5.2.3",
        "laravel-vite-plugin": "^1.3.0",
        "sass": "^1.56.1",
        "vite": "^6.3.5"
    },
    "dependencies": {
        "@fortawesome/fontawesome-free": "^7.2.0",
        "bootstrap-3-typeahead": "^4.0.2",
        "bootstrap5-autocomplete": "^1.1.42",
        "jquery": "^4.0.0",
        "terser": "^5.39.0",
        "typeahead-standalone": "^5.4.0"
    }
}
